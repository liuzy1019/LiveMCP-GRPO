# LiveMCP-GRPO dependencies for the verified ARL environment.
#
# Current A10 server:
#   nvcc 12.8 / CUDA Toolkit 12.8
#   PyTorch is installed from the official cu128 wheel index.
#
# 安装顺序（重要）：
#   1. 先装 PyTorch：
#      python -m pip install torch==2.10.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#   2. 再装 vLLM（让它自己解析依赖链，不要 pin flashinfer/compressed-tensors/xgrammar）：
#      python -m pip install vllm==0.19.1
#   3. 最后装项目其他依赖：
#      python -m pip install -r requirements.txt --no-build-isolation
#   4. verl 从源码安装：
#      python -m pip install -e ./verl
#
# flash-attn 当前未安装（vLLM V1 默认 flashinfer attention backend）。

# 核心
numpy==1.26.4
PyYAML==6.0.3
pydantic==2.12.5
loguru==0.7.3
packaging==25.0
einops>=0.8.0
scipy>=1.13.0
safetensors==0.7.0

# 配置（hydra 用于 GRPO 训练入口）
hydra-core==1.3.3
omegaconf>=2.3.0

# 数据
huggingface_hub==1.22.0
pyarrow==23.0.1
pandas>=2.2.3
datasets==4.8.4
transformers==5.13.0
tokenizers==0.22.2
protobuf>=5.29.6

# veRL training/model support (the project has no SFT route)
trl==0.29.1
peft==0.18.1
accelerate==1.14.0
tensorboard==2.20.0
wandb==0.26.0

# RL（vLLM 及其依赖已在步骤2安装，此处只声明不随 vLLM 变化的包）
ray==2.54.1
tensordict==0.10.0
xformers==0.0.32.post1
deepspeed==0.19.2
lm-format-enforcer==0.11.3

# 测试
pytest==9.1.0
