# Agent loop 注册表：verl worker 进程通过 agent_loop_config_path 加载
# 配合 actor_rollout_ref.rollout.agent.default_agent_loop 选择默认 loop

# OVAL Live MCP rollout + 审计
- name: livemcp_oval
  _target_: src.agent_loop.livemcp_oval_loop.LiveMCPOvalLoop
  # One model action is one XML tool call or one terminal response.  This is
  # deliberately independent from the 16k whole-trajectory response budget.
  max_action_tokens: 1024
